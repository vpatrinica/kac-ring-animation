%% 
%% This is file `acro.sty', generated 
%% on <1995/6/8> with the docstrip utility (2.2i).
%% 
%% The original source files were:
%% 
%% acro.dtx  (with options: `package')
%% 
%% Only a few abbreviated comments remain here to describe the usage. 
%% 
\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{acro}
      [1994/06/17 1.4 (PWD)]
 %-------------------------------------------------------------------
 % NOTICE:
 % This file may be used for non-profit purposes.
 % It may not be distributed in exchange for money,
 %   other than distribution costs.
 %
 % The author provides it `as is' and does not guarantee it in any way.
 %
%% Copyright (C) 1994 Patrick W. Daly
 % Max-Planck-Institut f\"ur Aeronomie
 % Postfach 20
 % D-37189 Katlenburg-Lindau
 % Germany
 %
 % E-mail:
 % SPAN--     nsp::linmpi::daly    (note nsp also known as ecd1)
 % Internet-- daly@linax1.dnet.gwdg.de
 %-----------------------------------------------------------
 % Acronym Use and Automatic Listing
 % ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 % All the acronyms may be stored in a common file for use with many documents
 % The definition is given as
 %         \newacro{TLA}{Three Letter Acronym}
 %     or  \newacro{TLA}[\sc tla]{Three Letter Acronym}
 %     or  \newacro{TLA}[\sc tla]{Three Letter Acronym}[3 Let. Acro.]
 % where the 1st optional argument in [..] specifies how the abbreviation is
 % to be output. Default is the acronym name itself (here TLA). The 2nd
 % optional argument is the text for the list of acronyms, if it differs
 % from the normal translation.
 %
 % For each acronym, there are two new commands, in this example
 %    \TLA   ==>>  TLA  (or in the 2nd case {\sc tla} )
 %    \trTLA ==>>  Three Letter Acronym  (translation of TLA)
 %
 % The starred version \TLA* does not print out the acronym, but does
 %   see that it is added to the list of acronyms.
 %
 % Put \listofacros in the document at the point where the list of acronyms
 % is to be output.
 %
 % The \TLA command also makes an entry in a glossary (.glo) file.
 % After the LaTeX processing, run
 %     makeindex -c -s acro.ist -o <filename>.iac <filename>
 % and reprocess LaTeX again. This time, the .iac file in read in by
 % \listofacros sandwiched between \preacro and \postacro, which may be
 % redefined as needed.
 % See documentation in the source .dtx file for more details.
 %-----------------------------------------------------------
\newcommand{\newacro}{}
\newcommand{\@newacro}{}
\newcommand{\@@newacro}{}

\def\newacro#1{\@ifnextchar[{\@newacro#1}{\@newacro#1[#1]}}
\def\@newacro#1[#2]#3{\@ifnextchar[{\@@newacro#1[#2]#3}%
     {\@@newacro#1[#2]#3[#3]}}
\def\@@newacro#1[#2]#3[#4]{%
  \@ifundefined{#1}{}{\PackageWarning{acro}
    {Acronym command \protect #1\space already exists\MessageBreak
     will be over-written}}
\expandafter\def\csname #1\endcsname{%
  \glossary{#1<#2>#4}\@ifnextchar*{\@gobble}{{#2}}}\expandafter
      \def\csname tr#1\endcsname{{#3}}}
\def\listofacros{%
  \IfFileExists{\jobname.iac}
  {\preacro \input{\jobname.iac} \postacro}
  {\PackageWarningNoLine{acro}
    {Cannot file file \jobname.iac\MessageBreak
     run makeindex -c -s acro.ist -o \jobname.iac \jobname.glo}}
}
\def\preacro{%
  \section*{\acroname}
  \addcontentsline{toc}{section}{\acroname}}

\def\postacro{\relax}
\def\acroname{List of Acronyms}
\AtBeginDocument{\makeglossary}
%% <<<<< End of decommented file <<<<<< 
%% 
%% End of file acro.sty. 
